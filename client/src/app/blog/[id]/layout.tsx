export const metadata = {
  title: "View Detail Blog",
  description: "This it Detail Blog page!",
};

export default function ViewDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
